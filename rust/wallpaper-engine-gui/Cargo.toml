[package]
name = "wallpaper-engine-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
rfd = "0.17.2"

shared = {path = "../shared"}
slint = "1.17.0"
tokio = {version = "1.52.3", features = ["full"]}

[build-dependencies]

slint-build = "1.17.0"
shared = {path = "../shared"}
