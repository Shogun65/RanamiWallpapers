[package]
name = "wllpaper-engine-client"
version = "0.1.0"
edition = "2024"

[dependencies]

shared = {path = "../shared"}
windows = {version = "0.62.2", features =[
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Shell",
    "Win32_Graphics_Gdi",
]}
